This project is a deployement of opensearch-operator stack threw argocd on k3s local cluster

This project depends on the [k3s],[argocd],[kyverno] and  [prometheus] repositories, which contain deployement of a local K3s cluster on WSL